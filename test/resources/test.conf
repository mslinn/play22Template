# This is the unit test configuration file.

sitePubConfigId = -123

db.default.driver=org.postgresql.Driver
db.default.user=postgres
db.default.password=hithere
db.default.url="jdbc:postgresql://localhost/slinnbooks"
db.default.url=${?DATABASE_URL}

enable_beta=true
enable_beta=${?ENABLE_BETA}

include "common.conf"
